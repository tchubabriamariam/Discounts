@model IEnumerable<Discounts.Application.DTOs.Categories.CategoryResponseDto>

@{
    ViewData["Title"] = "Category Management";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>Category Management</h1>
        <div class="d-flex gap-2">
            <a asp-action="Create" class="btn btn-primary">+ Create Category</a>
            <a asp-controller="Admin" asp-action="Dashboard" class="btn btn-outline-secondary">← Dashboard</a>
        </div>
    </div>

    <div class="table-responsive">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Description</th>
                    <th>Offers</th>
                    <th>Status</th>
                    <th>Created</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var category in Model)
                {
                    <tr class="@(!category.IsActive ? "table-secondary" : "")">
                        <td>
                            @if (!string.IsNullOrEmpty(category.IconUrl))
                            {
                                <img src="@category.IconUrl" alt="" style="width:20px; height:20px;" class="me-1" />
                            }
                            <strong>@category.Name</strong>
                        </td>
                        <td>
                            <small class="text-muted">@(category.Description ?? "-")</small>
                        </td>
                        <td>@category.OfferCount</td>
                        <td>
                            @if (category.IsActive)
                            {
                                <span class="badge bg-success">Active</span>
                            }
                            else
                            {
                                <span class="badge bg-secondary">Inactive</span>
                            }
                        </td>
                        <td><small>@category.CreatedAt.ToString("dd MMM yyyy")</small></td>
                        <td>
                            <div class="d-flex gap-1 flex-wrap">
                                <a asp-action="Edit" asp-route-id="@category.Id" class="btn btn-sm btn-outline-primary">Edit</a>

                                @if (category.IsActive)
                                {
                                    <form method="post" asp-action="Deactivate" asp-route-id="@category.Id" class="d-inline">
                                        @Html.AntiForgeryToken()
                                        <button type="submit" class="btn btn-sm btn-warning"
                                                onclick="return confirm('Deactivate this category?')">Deactivate</button>
                                    </form>
                                }
                                else
                                {
                                    <form method="post" asp-action="Activate" asp-route-id="@category.Id" class="d-inline">
                                        @Html.AntiForgeryToken()
                                        <button type="submit" class="btn btn-sm btn-success">Activate</button>
                                    </form>
                                }

                                @if (category.OfferCount == 0)
                                {
                                    <form method="post" asp-action="Delete" asp-route-id="@category.Id" class="d-inline">
                                        @Html.AntiForgeryToken()
                                        <button type="submit" class="btn btn-sm btn-danger"
                                                onclick="return confirm('Delete this category?')">Delete</button>
                                    </form>
                                }
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
